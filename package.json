{
  "name": "xule-site",
  "version": "1.0.0",
  "type": "module",
  "description": "Personal website - book aesthetic, algorithmic age",
  "scripts": {
    "start": "eleventy --serve",
    "build": "rm -rf _site && eleventy && bunx pagefind --site _site && bash scripts/indexnow-submit.sh"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-img": "^6.0.4",
    "markdown-it": "^14.0.0",
    "markdown-it-footnote": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/functions": "^3.4.2",
    "pagefind": "^1.4.0"
  }
}
