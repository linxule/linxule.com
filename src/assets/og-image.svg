<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630">
  <!-- Paper background -->
  <rect width="1200" height="630" fill="#f4f1eb"/>

  <!-- The accident: floating cyan dot -->
  <circle cx="360" cy="180" r="12" fill="#4ee1d4"/>

  <!-- Title: XULE LIN -->
  <text x="300" y="280"
        font-family="Cormorant Garamond, Georgia, serif"
        font-size="72"
        font-weight="600"
        letter-spacing="0.15em"
        fill="#1a1a1a">XULE LIN</text>

  <!-- Staggered statement - line 1 -->
  <text x="300" y="380"
        font-family="Cormorant Garamond, Georgia, serif"
        font-size="32"
        font-style="italic"
        font-weight="400"
        fill="#666666">Studying what happens when algorithms</text>

  <!-- Staggered statement - line 2 -->
  <text y="428"
        font-family="Cormorant Garamond, Georgia, serif"
        font-size="32"
        font-style="italic"
        font-weight="400">
    <tspan x="340" fill="#666666">shift from tools</tspan><tspan dx="8" fill="#4ee1d4">to participants.</tspan>
  </text>

  <!-- Subtle rule -->
  <line x1="300" y1="520" x2="580" y2="520" stroke="#cccccc" stroke-width="1"/>
</svg>
