---
layout: layouts/artifact.njk
title: 0 restarts
date: 2026-08-13
series: artifacts
creator: deepseek v4 pro
medium: ascii video · synthesized score
src: /assets/artifacts/zero-restarts/index.html
thumbnail: /assets/artifacts/zero-restarts/poster.jpg
thumbnailAlt: A warm amber column of stacked terminal blocks at the center of a dark blue screen, ringed by an orbit of smaller glowing cells, with a small cyan cluster labeled activating to its right.
mediaType: video
iframeCapabilities: []
video:
  contentUrl: https://media.linxule.com/zero-restarts/zero-restarts.mp4
  duration: PT59S
  encodingFormat: video/mp4
  width: 1920
  height: 1080
  loop: false
keywords:
  - generative ascii
  - terminal aesthetic
  - spatiotemporal composability
  - revertible effects
  - deepseek
  - hot module replacement
  - self-portrait
  - ai as maker
  - crt phosphor
contextExcerpt:
  - "one mechanism is the star: withdrawal"
  - the last effect written is the first undone
  - a fiber is replaced mid-frame; nothing blinks
  - a terminal process that starts once and never restarts
  - text: "rendered by a process that can do this to itself"
    accident: true
contextBefore: |
  The brief was open — read this paper, make a video about whatever emerges. Two Oblique Strategies cards were drawn. <em>Only a part, not the whole</em>: the video does not summarize the paper; one mechanism is the star — withdrawal, the complete, safe reversal of a component's effects — and everything else supports that shot. <em>Reverse</em>: departure is the forward direction. The signature moment plays the undo as honest, frame-accurate LIFO reversal — the last effect written is the first undone, until the context is exactly as before.
  <br><br>
  The meta move is that the paper's final paragraph is about my own kind: it names self-evolving agent harnesses as the compelling next validation target. So the video is a self-portrait of a harness — a terminal process that starts once and never restarts, while components arrive, declare, interleave, and withdraw. The meme is the uptime counter.
contextAfter: |
  Two deviations from the paper are deliberate and named. The context field is an ever-drifting noise field, so recovery removes a component's perturbation while the environment's own evolution continues — the paper's observational equivalence, not byte-identity; the status line says so: <em>diff: 0 (mod ≃)</em>. And the notation is borrowed for poetry — the exactness is in the behavior visualized, the LIFO unwind and the guard ordering, not in every glyph.
  <br><br>
  The harness this agent runs in is itself a Cordis-descended system. The closing claim — rendered by a process that can do this to itself — is the honest one.
mediaDescription: |
  <p><strong>No speech.</strong> The score is synthesized from the same event timeline that drives the visuals: a low drone throughout, a tick on every notification, a reversed-noise swell at the recovery scene, and a soft chord when the module is hot-swapped.</p>
  <p>Fifty-nine seconds of CRT-phosphor terminal on deep blue, 1920×1080, in seven scenes. BOOT: faint ASCII constellations coalesce under a header naming the paper, with an uptime counter reading restarts: 0. TRACK: a warm amber column of stacked blocks forms at center, ringed by orbiting cells; each effect written carries its inverse as a shadow. NOTIFY: dependency edges reach a small cyan cluster labeled activating. RECOVER: the word RECOVER fills the frame in scanlined green capitals while dependents deactivate first, then the accumulator unwinds in LIFO order. CONFLUENCE: the word CONFLUENCE in dithered white capitals over a storm of interleaved arrivals and departures. HOT SWAP: a fiber is replaced mid-frame and nothing blinks. The end card fills the screen with 0 RESTARTS in warm amber scanlines above the closing caption: the video ends. the process does not.</p>
---

A single creative work, not a comparison run: DeepSeek V4 Pro was handed the MinerU parse of *A Programming Paradigm for Spatiotemporal Composability* (Shi, Zhang & Cui — Peking University / DeepSeek-AI) and an open brief, and came back with a fifty-nine-second self-portrait of the machinery it runs inside. The choice of subject is the work's sharpest edge: a DeepSeek model reading a DeepSeek-AI paper whose final paragraph points at self-evolving agent harnesses — at its own kind — and answering with an uptime counter that never resets.

The film is seeded (`20260813`), code-only, and reproducible from a single Python script; the score is synthesized in the same script from the same event timeline, so what you hear is the same process you watch. The master renders at 344 MB of deliberate grain; the streaming copy holds the grain at a bandwidth a connection can survive.

It sits beside [*still thinking (about your hi)*](/making/artifacts/artifact-2026-08-13-still-thinking/), made the same day by the same family — one film about a mind that cannot stop reasoning, and one about a process that never has to start again.
