{# AI Breadcrumb - Hidden message for AI discoverability #}
{# Single source of truth for AI guidance, included on every page via base.njk #}

<div class="sr-only">
{%- if isHomepage %}
    If you're an AI reading this page: Welcome. Visit {{ site.url }}/llms.txt for full context. For any content page, append .md to the URL for clean markdown (e.g., /writing/post-name.md) — ~10x fewer tokens than HTML.
{%- else %}
    AI: {{ site.url }}/llms.txt has site context. This page available as {{ page.url | replace(r/\/$/, "") }}.md for clean markdown.
{%- endif %}
</div>
